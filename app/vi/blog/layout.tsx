// The blog's theme (v3 scope, fonts, and stylesheets) is set up in
// BlogShell so it applies to both the English `/blog` and Vietnamese
// `/vi/blog` segments from one place. This layout is just a pass-through.
export default function ViBlogLayout({ children }: { children: React.ReactNode }) {
  return children
}
